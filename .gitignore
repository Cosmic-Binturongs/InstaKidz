#These files are mostly auto-included by npx crate-react-app. Below are minor explanations of what they each are. Further answers can be found by googling "react [thing-name-here]"


# dependencies for react -- dont need to be inlcuded, gets included with npm install 
front-end/node_modules
/.pnp
.pnp.js

# testing files auto included in npx create. Jest testing framework (so far not using)
/coverage

# directory where production builds are built. It contains correctly bundled React in production mode and optimized builds.
/build

# .DS_Store gets generated by macOS users when viewing files in Finder. Not needed.
.DS_Store

# This is an environment file -- should not be included remotely for security purposes as per the use of .env
.env

#log files generated by npx create dealing with errors and debugging
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Byte-compiled, DLL files, or optimization files
__pycache__/
*.py[cod]

.vscode